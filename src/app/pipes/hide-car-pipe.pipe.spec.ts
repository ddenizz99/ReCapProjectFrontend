import { HideCarPipePipe } from './hide-car-pipe.pipe';

describe('HideCarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HideCarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
