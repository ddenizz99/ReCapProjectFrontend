import { CarLimitPipePipe } from './car-limit-pipe.pipe';

describe('CarLimitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CarLimitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
